route:
  receiver: operator-webhook
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operator-webhook
    webhook_configs:
      - url: https://alerts.example.invalid/shotlink
        send_resolved: true
